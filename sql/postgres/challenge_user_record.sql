CREATE TABLE challenge_user_record (
  challenge_record_actor INTEGER NOT NULL DEFAULT 0,
  challenge_wins INTEGER NOT NULL DEFAULT 0,
  challenge_losses INTEGER NOT NULL DEFAULT 0,
  challenge_ties INTEGER NOT NULL DEFAULT 0
);
